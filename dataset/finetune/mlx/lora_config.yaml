model: "mlx-community/Qwen2.5-3B-Instruct-4bit"
train: true
fine_tune_type: lora
optimizer: adamw
data: "dataset/finetune/data"
seed: 0

# Conservative for an 8GB unified-memory Mac -- this run is a cheap local
# sanity-check of the data/pipeline, not the final model (see cloud/ for that).
num_layers: 16
batch_size: 4
grad_accumulation_steps: 2
iters: 900
learning_rate: 1e-4
steps_per_report: 10
steps_per_eval: 200
adapter_path: "dataset/finetune/mlx/adapters"
save_every: 100
max_seq_length: 512
grad_checkpoint: true
mask_prompt: true

lora_parameters:
  keys: ["self_attn.q_proj", "self_attn.v_proj"]
  rank: 8
  scale: 20.0
  dropout: 0.1
